{
  "name": "viralis-render-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Standalone ffmpeg/yt-dlp worker that cuts, reframes, and captions Viralis clips. Deployed separately from the Next.js app (Vercel can't run ffmpeg).",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "@vercel/blob": "^0.27.1",
    "express": "^4.19.2"
  }
}
